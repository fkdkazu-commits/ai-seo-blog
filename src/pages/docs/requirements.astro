---
import DocsLayout from '../../layouts/DocsLayout.astro';
---

<DocsLayout
  title="要件定義書"
  description="AI SEO自動運用ブログシステムの目的・機能要件・非機能要件・コスト・開発フェーズを定義します。"
  docType="要件定義書"
  version="1.3"
  date="2026年5月24日"
>

  <!-- 目次 -->
  <div class="no-print bg-white rounded-xl border border-gray-200 p-6 mb-10">
    <h2 class="text-sm font-bold text-gray-500 uppercase tracking-wider mb-4">目次</h2>
    <ol class="grid md:grid-cols-2 gap-2 text-sm">
      {[
        ['1', 'プロジェクト概要'],
        ['2', '目的・背景'],
        ['3', '解決する課題'],
        ['4', '機能要件'],
        ['5', '非機能要件'],
        ['6', '利用技術'],
        ['7', 'コスト想定'],
        ['8', '開発フェーズ'],
        ['9', '制約事項・前提条件'],
      ].map(([num, label]) => (
        <li>
          <a href={`#section-${num}`} class="text-blue-600 hover:underline">
            {num}. {label}
          </a>
        </li>
      ))}
    </ol>
  </div>

  <div class="space-y-14">

    <!-- 1. プロジェクト概要 -->
    <section id="section-1">
      <h2 class="text-2xl font-bold text-gray-900 border-l-4 border-blue-500 pl-4 mb-6">1. プロジェクト概要</h2>
      <div class="bg-white rounded-xl border border-gray-200 p-6">
        <table class="w-full text-sm">
          <tbody class="divide-y divide-gray-100">
            {[
              ['プロジェクト名', 'AI SEO自動運用ブログシステム'],
              ['目的', 'AIによるSEO記事の自動生成・公開・分析・改善サイクルの完全自動化'],
              ['対象サイト', 'GitHub Pages（fkdkazu-commits.github.io/ai-seo-blog/）'],
              ['想定運用開始', '2026年5月（Phase 1 完了・稼働中）'],
              ['作成日', '2026年5月10日'],
              ['バージョン', 'v1.3'],
            ].map(([label, value]) => (
              <tr>
                <td class="py-3 pr-6 text-gray-500 font-medium w-40 shrink-0">{label}</td>
                <td class="py-3 text-gray-900">{value}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>

    </section>

    <!-- 2. 目的・背景 -->
    <section id="section-2">
      <h2 class="text-2xl font-bold text-gray-900 border-l-4 border-blue-500 pl-4 mb-6">2. 目的・背景</h2>
      <div class="space-y-4">
        <div class="bg-white rounded-xl border border-gray-200 p-6">
          <h3 class="font-bold text-gray-900 mb-3">2.1 背景</h3>
          <p class="text-gray-700 leading-relaxed text-sm">
            SEO集客はコンテンツ資産を継続的に積み上げることで効果を発揮しますが、記事執筆・分析・改善には多大な人的リソースが必要です。
            本プロジェクトでは <strong>Playwright（ブラウザ自動化）によるClaude.ai Web の自動操作</strong>を中核として、
            記事生成から内部リンク最適化までを自動化し、最小限の人手で継続的なSEO改善サイクルを実現します。
            Claude APIキーを使わずClaude Proサブスクリプション（月額固定）のみで動作するため、コスト予測が容易です。
          </p>
        </div>
        <div class="bg-white rounded-xl border border-gray-200 p-6">
          <h3 class="font-bold text-gray-900 mb-3">2.2 目的</h3>
          <ul class="space-y-2 text-sm text-gray-700">
            {[
              'SEO記事の自動生成・定期公開により、コンテンツ資産を継続的に積み上げる',
              'Google Search Console のデータを定期的に分析し、改善アクションを継続実行する',
              '人的工数を最小化しつつ、検索流入・CVRの向上を実現する',
              'Claude Proサブスクリプションのみで動作し、月額コストを固定・低コストに抑える',
            ].map((item) => (
              <li class="flex gap-3">
                <span class="text-blue-500 font-bold shrink-0">✓</span>
                {item}
              </li>
            ))}
          </ul>
        </div>
      </div>
    </section>

    <!-- 3. 解決する課題 -->
    <section id="section-3">
      <h2 class="text-2xl font-bold text-gray-900 border-l-4 border-blue-500 pl-4 mb-6">3. 解決する課題</h2>
      <div class="grid md:grid-cols-3 gap-4">
        {[
          {
            icon: '⏱️',
            problem: 'リソース不足',
            desc: '記事を継続的に書く時間・人手がない',
            solution: 'PlaywrightがClaude.ai Webを自動操作し、毎日記事を生成・公開',
          },
          {
            icon: '💰',
            problem: '高いAPI費用',
            desc: 'Claude APIの従量課金で月3〜20万円かかる',
            solution: 'Claude Proサブスクリプション（月約3,000円）のみで同等の自動化を実現',
          },
          {
            icon: '📊',
            problem: '分析・改善の停滞',
            desc: 'GSCデータを見て改善するサイクルが回っていない',
            solution: 'Search Console APIで毎日分析し、低パフォーマンス記事を自動で改善候補に抽出',
          },
        ].map(({ icon, problem, desc, solution }) => (
          <div class="bg-white rounded-xl border border-gray-200 p-5">
            <div class="text-3xl mb-3">{icon}</div>
            <h3 class="font-bold text-gray-900 mb-2">{problem}</h3>
            <p class="text-xs text-red-600 bg-red-50 rounded p-2 mb-3">課題: {desc}</p>
            <p class="text-xs text-green-700 bg-green-50 rounded p-2">解決: {solution}</p>
          </div>
        ))}
      </div>
    </section>

    <!-- 4. 機能要件 -->
    <section id="section-4">
      <h2 class="text-2xl font-bold text-gray-900 border-l-4 border-blue-500 pl-4 mb-6">4. 機能要件</h2>
      <div class="space-y-4">
        {[
          {
            id: 'F-01',
            category: '記事生成パイプライン（Playwright）',
            priority: '必須',
            requirements: [
              'キーワードリスト（keywords.json）から generated: false のキーワードを1件取得する',
              'PlaywrightがChromiumを起動し永続プロファイルでClaude.ai Webに自動ログインする',
              '5ステップのプロンプトを同一会話セッション内で順次実行する（前ステップの出力を次ステップに引き継ぐ）',
              '各ステップの出力をタグ（[SEO_RESEARCH_REPORT] / [ARTICLE_DRAFT]）でDOM Range APIを使いMarkdown形式で抽出する',
              'meta descriptionを自動生成する（120字以内）',
              '生成結果をMDX形式でsrc/content/blog/{slug}.mdxに保存する',
              '完了後にkeywords.jsonのgeneratedフラグをtrueに更新しコミットする',
            ],
          },
          {
            id: 'F-02',
            category: '自動公開',
            priority: '必須',
            requirements: [
              '生成した記事を自動でGitHub mainブランチへcommit・pushする',
              'pushを契機にGitHub Actionsが自動でビルド・GitHub Pagesへデプロイする',
              '毎日 AM 5:00（JST）に自動実行する',
            ],
          },
          {
            id: 'F-03',
            category: 'SEO分析',
            priority: '必須',
            requirements: [
              'Google Search Console APIから過去28日間のデータを取得する',
              '取得項目：クエリ・ページURL・クリック数・表示回数・CTR・掲載順位',
              'リライト候補を自動で抽出し、JSONファイルに出力する',
            ],
          },
          {
            id: 'F-04',
            category: '内部リンク最適化（Playwright）',
            priority: '必須',
            requirements: [
              '新規記事生成後、PlaywrightがClaude.aiで既存記事との関連性を分析する',
              '関連性の高い記事（最大3件）へのリンクを記事末尾に自動挿入する',
              '記事生成ワークフローと同一セッション内で実行しコストを抑える',
            ],
          },
          {
            id: 'F-05',
            category: 'OG画像生成',
            priority: '推奨',
            requirements: [
              '記事タイトルをもとにOGP画像（1200×630px）を自動生成する',
              '生成画像を public/og/ に保存し、記事と紐付ける',
              'sharpライブラリで実装しAPIコスト不要（Unsplash API優先・失敗時はSVGフォールバック）',
              '既存ファイルが存在する場合はスキップ（冪等性を確保）',
            ],
          },
          {
            id: 'F-06',
            category: 'AIリライト',
            priority: '推奨',
            requirements: [
              '表示回数100以上・CTR1.5%未満の記事のタイトル・descriptionを改善する',
              '掲載順位10位以下の記事の本文をリライトする（Phase 2以降）',
              'リライト後の記事を自動でGitHubへpush・公開する',
            ],
          },
          {
            id: 'F-07',
            category: '記事品質仕様（プロンプト設計）',
            priority: '必須',
            requirements: [
              'リード文冒頭50字以内に数値・期限・条件のいずれかを含める（インテントファースト）',
              'E-E-A-T準拠：体験談・具体数値を2件以上引用（URL付き）し、PRIMARY_SOURCESは.go.jp/.or.jp/大手メディアに限定する',
              '実務ノウハウを5個以上掲載し、隣接する2ハックは同一パターンを禁止する（A:失敗起点型 / B:データ起点型 / C:比較型）',
              'チェックリスト・2軸マトリクス・成熟度モデル・スコアリングのいずれか1つ以上の独自フレームを必須とする',
              'AIの常套句を禁止する（「〜とされています」「〜と言われています」「ぜひ〜してみてください」等）',
              '記事構成：H1 → リード文（400字以内）→ H2×3〜6（H3可）→ FAQ（3〜5問）→ まとめ（具体的行動）',
            ],
          },
          {
            id: 'F-08',
            category: 'チェーン整合性検証・自動回復',
            priority: '必須',
            requirements: [
              '各ステップ完了後に出力の文字数を検証する（Step1：300字以上 / Step2以降：1,500字以上）',
              'Step2以降：ターゲットキーワードが出力に含まれることを確認する',
              'Step2以降：Markdown見出し（##）が1つ以上含まれることを確認する',
              'DOM Range APIの成功 / plaintextフォールバック のどちらで抽出したかを記録・警告する',
              '各ステップの出力をdata/debug/にタイムスタンプ付きで保存する（.gitignore済み）',
              'Claude.aiが質問返し（「どこから着手しましょうか」等）をした場合を検知し、回復プロンプトを自動送信する',
            ],
          },
        ].map(({ id, category, priority, requirements }) => (
          <div class="bg-white rounded-xl border border-gray-200 overflow-hidden">
            <div class="flex items-center gap-4 px-6 py-4 bg-gray-50 border-b border-gray-200">
              <span class="text-xs font-mono text-gray-500">{id}</span>
              <h3 class="font-bold text-gray-900">{category}</h3>
              <span class={`text-xs px-2 py-0.5 rounded-full font-medium ml-auto ${priority === '必須' ? 'bg-red-100 text-red-700' : 'bg-yellow-100 text-yellow-700'}`}>
                {priority}
              </span>
            </div>
            <ul class="px-6 py-4 space-y-2">
              {requirements.map((req) => (
                <li class="text-sm text-gray-700 flex gap-2">
                  <span class="text-gray-400 shrink-0">•</span>
                  {req}
                </li>
              ))}
            </ul>
          </div>
        ))}
      </div>

      <!-- 5ステップパイプライン詳細 -->
      <div class="mt-6 bg-white rounded-xl border border-gray-200 overflow-hidden">
        <div class="px-6 py-4 bg-gray-50 border-b border-gray-200">
          <h3 class="font-bold text-gray-900">F-01 補足：5ステップパイプライン仕様</h3>
          <p class="text-xs text-gray-500 mt-1">同一Claude.ai会話内で順次実行し、前ステップの出力を次ステップの文脈として引き継ぐ</p>
        </div>
        <div class="overflow-x-auto">
          <table class="w-full text-sm">
            <thead class="bg-gray-50 border-b border-gray-200">
              <tr>
                <th class="text-left px-6 py-3 text-gray-600 font-semibold w-20">Step</th>
                <th class="text-left px-6 py-3 text-gray-600 font-semibold">処理内容</th>
                <th class="text-left px-6 py-3 text-gray-600 font-semibold w-40">出力タグ</th>
                <th class="text-left px-6 py-3 text-gray-600 font-semibold w-28">タイムアウト</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-gray-100">
              {[
                ['Step 1', 'SEOリサーチ（競合分析・一次情報収集・検索意図分類）', '[SEO_RESEARCH_REPORT]', '10分'],
                ['Step 2', '記事生成（8,000字以上・E-E-A-T準拠・独自フレーム含む）', '[ARTICLE_DRAFT]', '30分'],
                ['Step 3', 'ファクトチェック（URL検証・数値・固有名詞の確認）', '[ARTICLE_DRAFT]', '20分'],
                ['Step 4', '品質向上（AI文体除去・テンプレ感排除・まとめ改善）', '[ARTICLE_DRAFT]', '15分'],
                ['Step 5', '内部リンク挿入（既存記事との相互リンク追加）', '[ARTICLE_DRAFT]', '10分'],
              ].map(([step, desc, tag, timeout]) => (
                <tr>
                  <td class="px-6 py-4 font-medium text-blue-700">{step}</td>
                  <td class="px-6 py-4 text-gray-700">{desc}</td>
                  <td class="px-6 py-4 font-mono text-xs text-gray-500">{tag}</td>
                  <td class="px-6 py-4 text-gray-700">{timeout}</td>
                </tr>
              ))}
              <tr class="bg-gray-50">
                <td class="px-6 py-4 font-bold text-gray-900" colspan="3">GitHub Actions ジョブ全体タイムアウト</td>
                <td class="px-6 py-4 font-bold text-gray-900">150分</td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>
    </section>

    <!-- 5. 非機能要件 -->
    <section id="section-5">
      <h2 class="text-2xl font-bold text-gray-900 border-l-4 border-blue-500 pl-4 mb-6">5. 非機能要件</h2>
      <div class="bg-white rounded-xl border border-gray-200 overflow-hidden">
        <table class="w-full text-sm">
          <thead class="bg-gray-50 border-b border-gray-200">
            <tr>
              <th class="text-left px-6 py-3 text-gray-600 font-semibold w-32">区分</th>
              <th class="text-left px-6 py-3 text-gray-600 font-semibold">要件</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-gray-100">
            {[
              ['可用性', 'GitHub Pages の SLA に準拠（月次 99.9% 以上を目標）'],
              ['パフォーマンス', 'Core Web Vitals: LCP 2.5秒以下、CLS 0.1以下（Astro静的生成で達成）'],
              ['タイムアウト', '各ステップ個別タイムアウトあり（Step1:10分 / Step2:30分 / Step3:20分 / Step4:15分 / Step5:10分）。GitHub Actionsジョブ全体は150分'],
              ['ブラウザ設定', 'ヘッドレスOFF・User-Agent偽装（Chrome/124 Windows NT 10.0）・自動化検出フラグ無効・ロケールja-JP・ビューポート1280×900px'],
              ['スケーラビリティ', 'キーワードリストを追加するだけで記事数を無制限に拡張可能'],
              ['セキュリティ', 'APIキー・セッション情報はプロジェクトフォルダ外の専用フォルダで管理。コードへの埋め込み禁止。ダッシュボードはlocalhost（127.0.0.1）のみバインド'],
              ['保守性', 'すべての自動化はGitHub Actionsで管理。設定変更はYAMLファイルのみ'],
              ['コスト管理', 'Claude Proサブスクリプション（月額固定）のため従量課金リスクなし'],
              ['デバッグ', 'ステップ出力をdata/debug/に保存（タイムスタンプ・DOM Range成功 / PLAINTEXTフォールバックをファイル名で区別）。.gitignore済みのためリポジトリには含まれない'],
              ['ログ・監視', 'GitHub Actionsのログで自動化の実行状況をリアルタイム確認可能'],
            ].map(([label, value]) => (
              <tr>
                <td class="px-6 py-4 text-gray-500 font-medium">{label}</td>
                <td class="px-6 py-4 text-gray-700">{value}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </section>

    <!-- 6. 利用技術 -->
    <section id="section-6">
      <h2 class="text-2xl font-bold text-gray-900 border-l-4 border-blue-500 pl-4 mb-6">6. 利用技術</h2>
      <div class="grid md:grid-cols-2 gap-4">
        {[
          {
            layer: 'フロントエンド',
            items: [
              'Astro 4.x（静的サイトジェネレーター）',
              'MDX（記事フォーマット）',
              'Tailwind CSS（スタイリング）',
            ],
          },
          {
            layer: 'ホスティング・CI/CD',
            items: [
              'GitHub Pages（ホスティング・無料）',
              'GitHub Actions（自動化パイプライン）',
              'GitHub（ソースコード・記事管理）',
            ],
          },
          {
            layer: 'AI自動化（APIキー不要）',
            items: [
              'Playwright 1.44+（Chromiumブラウザ自動化）',
              'Claude.ai Web（Claude Proサブスクリプション）',
              'Playwright永続プロファイルによる認証（セッション自動再利用）',
            ],
          },
          {
            layer: 'SEO分析・ユーティリティ',
            items: [
              'Google Search Console API v1（SEO分析）',
              'googleapis（Node.js クライアント）',
              'TypeScript + tsx（スクリプト実装）',
              'sharp（OG画像生成・無料）',
            ],
          },
        ].map(({ layer, items }) => (
          <div class="bg-white rounded-xl border border-gray-200 p-5">
            <h3 class="font-bold text-gray-900 mb-3 text-sm">{layer}</h3>
            <ul class="space-y-2">
              {items.map((item) => (
                <li class="text-sm text-gray-700 flex gap-2">
                  <span class="text-blue-400 shrink-0">▸</span>
                  {item}
                </li>
              ))}
            </ul>
          </div>
        ))}
      </div>
    </section>

    <!-- 7. コスト想定 -->
    <section id="section-7">
      <h2 class="text-2xl font-bold text-gray-900 border-l-4 border-blue-500 pl-4 mb-6">7. コスト想定</h2>

      <!-- コストサマリー -->
      <div class="bg-green-50 border border-green-200 rounded-xl p-5 mb-6">
        <p class="text-xs font-bold text-green-600 uppercase tracking-wider mb-1">月額運用コスト（目安）</p>
        <p class="text-3xl font-bold text-green-600 mb-1">約¥3,000<span class="text-base font-normal text-green-400">/月</span></p>
        <p class="text-xs text-green-600">月額固定（Playwright + Claude Pro）・記事数が増えてもコスト変動なし</p>
      </div>

      <div class="grid md:grid-cols-2 gap-6">
        <div class="bg-white rounded-xl border border-gray-200 overflow-hidden">
          <div class="px-6 py-4 bg-gray-50 border-b border-gray-200">
            <h3 class="font-bold text-gray-900">初期費用</h3>
          </div>
          <table class="w-full text-sm">
            <tbody class="divide-y divide-gray-100">
              {[
                ['Astro / GitHub / GitHub Pages', '無料'],
                ['Playwright（OSS）', '無料'],
                ['ドメイン（任意）', '約1,000〜2,000円/年'],
                ['開発・設定費', '別途見積'],
              ].map(([item, cost]) => (
                <tr>
                  <td class="px-6 py-3 text-gray-700">{item}</td>
                  <td class="px-6 py-3 text-right font-medium text-gray-900">{cost}</td>
                </tr>
              ))}
              <tr class="bg-blue-50">
                <td class="px-6 py-3 font-bold text-gray-900">合計（ドメインなし）</td>
                <td class="px-6 py-3 text-right font-bold text-blue-600">¥0</td>
              </tr>
            </tbody>
          </table>
        </div>
        <div class="bg-white rounded-xl border border-gray-200 overflow-hidden">
          <div class="px-6 py-4 bg-gray-50 border-b border-gray-200">
            <h3 class="font-bold text-gray-900">月額運用費</h3>
          </div>
          <table class="w-full text-sm">
            <tbody class="divide-y divide-gray-100">
              {[
                ['GitHub Pages', '無料'],
                ['GitHub Actions', '無料（月2,000分まで）'],
                ['Claude Pro サブスクリプション', '約3,000円/月'],
                ['ドメイン', '約100〜200円/月'],
              ].map(([item, cost]) => (
                <tr>
                  <td class="px-6 py-3 text-gray-700">{item}</td>
                  <td class="px-6 py-3 text-right font-medium text-gray-900">{cost}</td>
                </tr>
              ))}
              <tr class="bg-green-50">
                <td class="px-6 py-3 font-bold text-gray-900">月額合計（目安）</td>
                <td class="px-6 py-3 text-right font-bold text-green-600">約¥3,000〜¥3,200</td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>
      <p class="text-xs text-gray-500 mt-3">
        ※ Claude Proサブスクリプションの料金は変更される場合があります。GitHub Actionsは月2,000分を超えると従量課金となりますが、通常の運用では超過しません。
      </p>
    </section>

    <!-- 8. 開発フェーズ -->
    <section id="section-8">
      <h2 class="text-2xl font-bold text-gray-900 border-l-4 border-blue-500 pl-4 mb-6">8. 開発フェーズ</h2>
      <div class="space-y-4">
        {[
          {
            phase: 'Phase 1',
            subtitle: 'MVP（最小構成）',
            period: '〜 2週間',
            color: 'blue',
            status: '実装済み',
            items: [
              'Astro + GitHub Pages 環境構築',
              'MDX記事の表示・ルーティング',
              'Playwright + Claude.ai による5ステップ記事自動生成',
              'GitHub Actions による自動投稿・デプロイ',
              'OG画像自動生成・内部リンク自動追加',
              'チェーン整合性検証・デバッグ出力機能',
              'プロンプト品質改善（E-E-A-T・A/B/Cパターン・独自フレーム必須化）',
            ],
          },
          {
            phase: 'Phase 2',
            subtitle: 'SEO分析・改善ループ',
            period: '〜 1ヶ月',
            color: 'purple',
            status: '実装中',
            items: [
              'Google Search Console API 連携',
              'CTR・順位データの自動分析',
              'リライト候補の自動抽出・JSONファイル出力',
              'Playwright + Claude.ai による自動リライト（実装中）',
            ],
          },
          {
            phase: 'Phase 3',
            subtitle: '品質向上・高度化',
            period: '〜 2ヶ月',
            color: 'green',
            status: '計画中',
            items: [
              'sitemap.xml 自動生成',
              'パフォーマンスモニタリング強化',
              'キーワード候補の自動提案',
              'Core Web Vitals 最適化',
            ],
          },
          {
            phase: 'Phase 4',
            subtitle: 'AIエージェント化',
            period: '3ヶ月以降',
            color: 'orange',
            status: '将来計画',
            items: [
              '競合サイト・SERP自動分析',
              'AIによる戦略提案・Slack通知',
              '自動レポート生成',
              '完全自律SEO運用の実現',
            ],
          },
        ].map(({ phase, subtitle, period, color, status, items }) => (
          <div class={`bg-white rounded-xl border-2 ${color === 'blue' ? 'border-blue-200' : color === 'purple' ? 'border-purple-200' : color === 'green' ? 'border-green-200' : 'border-orange-200'} overflow-hidden`}>
            <div class={`flex items-center justify-between px-6 py-4 ${color === 'blue' ? 'bg-blue-50' : color === 'purple' ? 'bg-purple-50' : color === 'green' ? 'bg-green-50' : 'bg-orange-50'}`}>
              <div class="flex items-center gap-3">
                <span class={`font-bold text-sm ${color === 'blue' ? 'text-blue-700' : color === 'purple' ? 'text-purple-700' : color === 'green' ? 'text-green-700' : 'text-orange-700'}`}>{phase}</span>
                <h3 class="font-bold text-gray-900">{subtitle}</h3>
              </div>
              <div class="flex items-center gap-3 text-xs">
                <span class="text-gray-500">{period}</span>
                <span class={`px-2 py-0.5 rounded-full font-medium ${status === '実装済み' ? 'bg-green-100 text-green-700' : status === '実装中' ? 'bg-blue-100 text-blue-700' : status === '計画中' ? 'bg-yellow-100 text-yellow-700' : 'bg-gray-100 text-gray-600'}`}>
                  {status}
                </span>
              </div>
            </div>
            <ul class="px-6 py-4 grid md:grid-cols-2 gap-2">
              {items.map((item) => (
                <li class="text-sm text-gray-700 flex gap-2">
                  <span class="text-gray-400 shrink-0">•</span>
                  {item}
                </li>
              ))}
            </ul>
          </div>
        ))}
      </div>
    </section>

    <!-- 9. 制約事項 -->
    <section id="section-9">
      <h2 class="text-2xl font-bold text-gray-900 border-l-4 border-blue-500 pl-4 mb-6">9. 制約事項・前提条件</h2>
      <div class="grid md:grid-cols-2 gap-4">
        <div class="bg-white rounded-xl border border-gray-200 p-6">
          <h3 class="font-bold text-gray-900 mb-4">制約事項</h3>
          <ul class="space-y-3 text-sm text-gray-700">
            {[
              'GitHub Pages はサーバーサイド処理不可（静的サイトのみ）',
              'Google Search Console APIはサイト認証後（Googleサーチコンソールへの登録）が必要',
              'Claude.aiのPlaywright永続プロファイルは定期的に期限切れとなり、再ログインが必要（目安：数週間〜数ヶ月）',
              '生成記事の品質はキーワードの精度・プロンプト設計に依存する',
              'GitHub Actions の実行時間は月2,000分まで無料（通常の運用では超過しない）',
              'DOM Range APIによるMarkdown抽出が失敗した場合、見出し（##）が消えたプレーンテキストとなる（チェーン整合性検証で検知・警告）',
              'パイプライン中断時はkeywords.jsonのgeneratedフラグを手動でfalseに戻して再実行が必要',
              '質問返し検知は1回の回復プロンプト送信のみ対応（複数回の質問返しが続く場合はタイムアウト）',
            ].map((item) => (
              <li class="flex gap-2">
                <span class="text-red-400 shrink-0">!</span>
                {item}
              </li>
            ))}
          </ul>
        </div>
        <div class="bg-white rounded-xl border border-gray-200 p-6">
          <h3 class="font-bold text-gray-900 mb-4">前提条件</h3>
          <ul class="space-y-3 text-sm text-gray-700">
            {[
              'GitHubアカウントが存在すること',
              'Claude Proサブスクリプションを契約済みであること（APIキー不要）',
              'Google Search Console にサイトが登録済みであること（Phase 2以降）',
              'Playwright永続プロファイルの初回セットアップ（Claude.aiへの手動ログイン）が完了していること',
              'キーワードリスト（data/keywords/keywords.json）を定期的に更新すること',
              'セルフホストランナー（actions-runner/）が起動済みであること',
            ].map((item) => (
              <li class="flex gap-2">
                <span class="text-green-500 shrink-0">✓</span>
                {item}
              </li>
            ))}
          </ul>
        </div>
      </div>
    </section>

  </div>

  <!-- Navigation -->
  <div class="no-print flex justify-between items-center mt-16 pt-8 border-t border-gray-200">
    <a href={`${import.meta.env.BASE_URL}docs/`} class="text-sm text-gray-500 hover:text-blue-600">← 資料一覧に戻る</a>
    <a href={`${import.meta.env.BASE_URL}docs/design/`} class="text-sm bg-blue-600 text-white px-6 py-2 rounded-lg hover:bg-blue-700 transition-colors">
      設計書を見る →
    </a>
  </div>

</DocsLayout>
